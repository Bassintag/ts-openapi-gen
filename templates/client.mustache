/* Generated by ts-codegen, do not edit manually */

import { {{#endpoints}}{{definition.name}}, {{/endpoints}} } from '.';

export interface ClientOptions {
    baseUrl?: string;
}

export class Client {

    {{#endpoints}}
    readonly {{name}}: {{definition.name}};

    {{/endpoints}}
    constructor({ baseUrl }: ClientOptions = {}) {
        {{#endpoints}}
        this.{{name}} = new {{definition.name}}(baseUrl);
        {{/endpoints}}
    }
}
