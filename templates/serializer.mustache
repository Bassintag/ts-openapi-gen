/* Generated by ts-openapi-gen, do not edit manually */

import { {{name}}, {{#dependencies}}serialize{{.}}, {{/dependencies}}} from '..';

{{#literalType}}
export const serialize{{name}} = (value: {{name}}): {{name}} => value;
{{/literalType}}
{{^literalType}}
export const serialize{{name}} = (value: {{name}}): {{name}} => {
    return {
{{#properties}}
    {{#model.name}}
        {{name}}: {{^isRequired}}value.{{name}} == null ? value.{{name}} : {{/isRequired}}serialize{{model.name}}(value.{{name}}),
    {{/model.name}}
    {{^model.name}}
        {{name}}: value.{{name}},
    {{/model.name}}
{{/properties}}
    };
}
{{/literalType}}
